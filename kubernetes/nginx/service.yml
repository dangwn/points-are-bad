apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: points-are-bad
  labels:
    app: points-are-bad
spec:
  type: NodePort
  selector:
    app: points-are-bad-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30009